-- fpga_hashjoin--1.0.sql
--
-- This extension installs no SQL objects.
-- All functionality is provided via PostgreSQL hooks registered in _PG_init().
-- The extension must be loaded via shared_preload_libraries or LOAD command.

-- complain if script is sourced in psql, rather than via CREATE EXTENSION
\echo Use "CREATE EXTENSION fpga_hashjoin" to load this file. \quit
